<UserControl x:Class="SpriteTool.TrackPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpriteTool"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Add">
                
                <!-- Open a message box asking for a track name -->
                <MenuItem Header="AnimationTrack"/>
            </MenuItem>
        </Menu>
        <Border Grid.Row="2" Background="{StaticResource Background4}">
            <ScrollViewer>
                <StackPanel Name="Sp_Tracks"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
